"use client";

import { useEffect, useState } from "react";

import AdminUnlockGate from "@/components/admin/AdminUnlockGate";
import { getDashboardAuthHeaders } from "@/lib/security/dashboardClientAuth";
import { getClientSiteSlug } from "@/lib/site/siteConfig";

type Service = { id: number; title: string; description: string | null; price: number | null; duration: number | null; sort_order?: number | null };
type Variation = { id: number; service_id: number; variation_name: string; price: number | null; duration: number | null };
type FormState = { title: string; description: string; price: string; duration: string };
type VariationFormState = { service_id: string; variation_name: string; price: string; duration: string };

const fixMyCrownSlug = "fix-my-crown";
const blankForm: FormState = { title: "", description: "", price: "", duration: "" };
const blankVariationForm: VariationFormState = { service_id: "", variation_name: "", price: "", duration: "" };
const starterHairServices: FormState[] = [
  { title: "Silk Press", description: "Smooth silk press styling appointment with a polished finish.", price: "85", duration: "120" },
  { title: "Wig Install", description: "Wig install appointment with styling and finishing details.", price: "125", duration: "150" },
  { title: "Quick Weave", description: "Quick weave styling appointment for a fresh, confident look.", price: "100", duration: "150" },
  { title: "Wash, Treatment & Style", description: "Wash, conditioning treatment, and finished style appointment.", price: "95", duration: "150" },
];

function servicePayload(input: FormState | Service) {
  return { title: String(input.title ?? "").trim(), description: String(input.description ?? "").trim(), price: Number(input.price ?? 0), duration: Number(input.duration ?? 60), sort_order: Number("sort_order" in input ? input.sort_order ?? 100 : 100) };
}
function variationPayload(input: VariationFormState | Variation) {
  return { service_id: Number(input.service_id ?? 0), variation_name: String(input.variation_name ?? "").trim(), price: Number(input.price ?? 0), duration: Number(input.duration ?? 60) };
}

export default function ServiceManagerPage() {
  const siteSlug = getClientSiteSlug();
  const canUseHairStarter = siteSlug === fixMyCrownSlug;
  const [services, setServices] = useState<Service[]>([]);
  const [variations, setVariations] = useState<Variation[]>([]);
  const [form, setForm] = useState<FormState>(blankForm);
  const [variationForm, setVariationForm] = useState<VariationFormState>(blankVariationForm);
  const [loading, setLoading] = useState(true);
  const [busy, setBusy] = useState(false);
  const [activeId, setActiveId] = useState<number | null>(null);
  const [message, setMessage] = useState("");
  const [error, setError] = useState("");

  async function api(path: string, init?: RequestInit) {
    const response = await fetch(path, { ...init, headers: { ...(init?.body ? { "Content-Type": "application/json" } : {}), ...getDashboardAuthHeaders(), ...(init?.headers ?? {}) } });
    const data = await response.json().catch(() => ({}));
    if (!response.ok) throw new Error(data.error ?? "Request failed.");
    return data;
  }

  async function loadAll() {
    try {
      setLoading(true); setError("");
      const [serviceData, variationData] = await Promise.all([api("/api/dashboard/services"), api("/api/dashboard/service-variations")]);
      const nextServices = (serviceData.services ?? []) as Service[];
      setServices(nextServices);
      setVariations((variationData.variations ?? []) as Variation[]);
      setVariationForm((current) => ({ ...current, service_id: current.service_id || String(nextServices[0]?.id ?? "") }));
    } catch (loadError) { setError(loadError instanceof Error ? loadError.message : "Services could not be loaded."); }
    finally { setLoading(false); }
  }
  useEffect(() => { loadAll(); }, []);

  async function saveServiceOrder(nextServices: Service[]) {
    try { setBusy(true); setError(""); setMessage(""); await api("/api/dashboard/services", { method: "PATCH", body: JSON.stringify({ ordered_ids: nextServices.map((service) => service.id) }) }); setServices(nextServices.map((service, index) => ({ ...service, sort_order: (index + 1) * 10 }))); setMessage("Service order saved."); await loadAll(); }
    catch (orderError) { setError(orderError instanceof Error ? orderError.message : "Service order could not be saved."); }
    finally { setBusy(false); }
  }
  async function moveService(index: number, direction: "up" | "down") { const nextIndex = direction === "up" ? index - 1 : index + 1; if (nextIndex < 0 || nextIndex >= services.length) return; const nextServices = [...services]; const [service] = nextServices.splice(index, 1); nextServices.splice(nextIndex, 0, service); await saveServiceOrder(nextServices); }
  async function createService(input: FormState) { const payload = servicePayload(input); if (!payload.title || payload.price <= 0 || payload.duration <= 0) throw new Error("Service title, price, and duration are required."); await api("/api/dashboard/services", { method: "POST", body: JSON.stringify({ ...payload, sort_order: (services.length + 1) * 10 }) }); }
  async function handleCreate() { try { setBusy(true); setError(""); setMessage(""); await createService(form); setForm(blankForm); setMessage("Service created."); await loadAll(); } catch (createError) { setError(createError instanceof Error ? createError.message : "Service could not be created."); } finally { setBusy(false); } }
  async function createVariation(input: VariationFormState) { const payload = variationPayload(input); if (!payload.service_id || !payload.variation_name || payload.price <= 0 || payload.duration <= 0) throw new Error("Service, variation name, price, and duration are required."); await api("/api/dashboard/service-variations", { method: "POST", body: JSON.stringify(payload) }); }
  async function handleCreateVariation() { try { setBusy(true); setError(""); setMessage(""); await createVariation(variationForm); setVariationForm((current) => ({ ...blankVariationForm, service_id: current.service_id })); setMessage("Variation created."); await loadAll(); } catch (createError) { setError(createError instanceof Error ? createError.message : "Variation could not be created."); } finally { setBusy(false); } }
  async function addStarterHairServices() { if (!canUseHairStarter) { setError("Starter hair services only run for Iyanla Fix My Crown."); return; } try { setBusy(true); setError(""); setMessage(""); for (const starterService of starterHairServices) { const exists = services.some((service) => service.title.toLowerCase() === starterService.title.toLowerCase()); if (!exists) await createService(starterService); } setMessage("Starter hair services added."); await loadAll(); } catch (starterError) { setError(starterError instanceof Error ? starterError.message : "Starter hair services could not be added."); } finally { setBusy(false); } }
  async function saveService(service: Service) { try { setActiveId(service.id); setError(""); setMessage(""); await api("/api/dashboard/services", { method: "PUT", body: JSON.stringify({ id: service.id, ...servicePayload(service) }) }); setMessage("Service saved."); await loadAll(); } catch (saveError) { setError(saveError instanceof Error ? saveError.message : "Service could not be saved."); } finally { setActiveId(null); } }
  async function saveVariation(variation: Variation) { try { setActiveId(variation.id); setError(""); setMessage(""); await api("/api/dashboard/service-variations", { method: "PUT", body: JSON.stringify({ id: variation.id, ...variationPayload(variation) }) }); setMessage("Variation saved."); await loadAll(); } catch (saveError) { setError(saveError instanceof Error ? saveError.message : "Variation could not be saved."); } finally { setActiveId(null); } }
  async function deleteService(id: number) { try { setActiveId(id); setError(""); setMessage(""); await api(`/api/dashboard/services?id=${id}`, { method: "DELETE" }); setMessage("Service deleted."); await loadAll(); } catch (deleteError) { setError(deleteError instanceof Error ? deleteError.message : "Service could not be deleted."); } finally { setActiveId(null); } }
  async function deleteVariation(id: number) { try { setActiveId(id); setError(""); setMessage(""); await api(`/api/dashboard/service-variations?id=${id}`, { method: "DELETE" }); setMessage("Variation deleted."); await loadAll(); } catch (deleteError) { setError(deleteError instanceof Error ? deleteError.message : "Variation could not be deleted."); } finally { setActiveId(null); } }
  function updateLocal(id: number, updates: Partial<Service>) { setServices((current) => current.map((service) => (service.id === id ? { ...service, ...updates } : service))); }
  function updateLocalVariation(id: number, updates: Partial<Variation>) { setVariations((current) => current.map((variation) => (variation.id === id ? { ...variation, ...updates } : variation))); }

  return <AdminUnlockGate title="Service Manager"><main className="min-h-screen bg-black px-6 py-10 text-white md:px-10"><div className="mx-auto grid max-w-6xl gap-8"><div className="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="mb-4 text-sm uppercase tracking-[0.3em] text-zinc-500">Dashboard · {siteSlug}</p><h1 className="text-5xl font-black">Service Manager</h1><p className="mt-4 max-w-2xl text-zinc-400">Create, edit, and reorder services. Deposits are managed separately in Add-ons & Deposit Rules.</p></div>{canUseHairStarter && <button type="button" onClick={addStarterHairServices} disabled={busy || loading} className="rounded-full border border-white/10 px-5 py-3 text-sm font-black text-white disabled:opacity-60">Add Starter Hair Services</button>}</div>{error && <div className="rounded-3xl border border-red-500/30 bg-red-500/10 p-4 text-red-200">{error}</div>}{message && <div className="rounded-3xl border border-green-500/30 bg-green-500/10 p-4 text-green-200">{message}</div>}<section className="rounded-3xl border border-white/10 bg-white/5 p-6"><h2 className="text-2xl font-black">Create Service</h2><div className="mt-5 grid gap-4 md:grid-cols-2"><Field label="Service Title" value={form.title} onChange={(value) => setForm((current) => ({ ...current, title: value }))} /><Field label="Price" value={form.price} onChange={(value) => setForm((current) => ({ ...current, price: value }))} type="number" /><Field label="Duration Minutes" value={form.duration} onChange={(value) => setForm((current) => ({ ...current, duration: value }))} type="number" /><label className="grid gap-2 md:col-span-2"><span className="text-sm text-zinc-400">Description</span><textarea value={form.description} onChange={(event) => setForm((current) => ({ ...current, description: event.target.value }))} className="min-h-24 rounded-xl border border-white/10 bg-black px-4 py-3" /></label></div><button type="button" onClick={handleCreate} disabled={busy} className="mt-5 rounded-full bg-white px-6 py-3 text-sm font-black text-black disabled:opacity-60">Create Service</button></section><section className="rounded-3xl border border-white/10 bg-white/5 p-6"><h2 className="text-2xl font-black">Create Variation</h2><p className="mt-2 text-sm text-zinc-400">Attach a variation to one service. Deposit rules still control deposits.</p><div className="mt-5 grid gap-4 md:grid-cols-2"><label className="grid gap-2"><span className="text-sm text-zinc-400">Parent Service</span><select value={variationForm.service_id} onChange={(event) => setVariationForm((current) => ({ ...current, service_id: event.target.value }))} className="rounded-xl border border-white/10 bg-black px-4 py-3"><option value="">Choose service</option>{services.map((service) => <option key={service.id} value={service.id}>{service.title}</option>)}</select></label><Field label="Variation Name" value={variationForm.variation_name} onChange={(value) => setVariationForm((current) => ({ ...current, variation_name: value }))} /><Field label="Price" value={variationForm.price} onChange={(value) => setVariationForm((current) => ({ ...current, price: value }))} type="number" /><Field label="Duration Minutes" value={variationForm.duration} onChange={(value) => setVariationForm((current) => ({ ...current, duration: value }))} type="number" /></div><button type="button" onClick={handleCreateVariation} disabled={busy || services.length === 0} className="mt-5 rounded-full bg-white px-6 py-3 text-sm font-black text-black disabled:opacity-60">Create Variation</button></section>{loading ? <div className="rounded-3xl border border-white/10 bg-white/5 p-8 text-zinc-400">Loading services...</div> : services.length === 0 ? <div className="rounded-3xl border border-dashed border-white/10 bg-white/5 p-8 text-center text-zinc-400">No services yet.</div> : <section className="grid gap-6">{services.map((service, index) => { const serviceVariations = variations.filter((variation) => variation.service_id === service.id); return <div key={service.id} className="rounded-3xl border border-white/10 bg-white/5 p-6"><div className="mb-5 flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-white/10 bg-black/40 p-4"><div><p className="text-sm uppercase tracking-[0.2em] text-zinc-500">Listing Order</p><p className="text-lg font-black">#{index + 1}</p></div><div className="flex gap-2"><button type="button" disabled={busy || index === 0} onClick={() => moveService(index, "up")} className="rounded-full border border-white/10 px-4 py-2 text-sm font-black disabled:opacity-40">Move Up</button><button type="button" disabled={busy || index === services.length - 1} onClick={() => moveService(index, "down")} className="rounded-full border border-white/10 px-4 py-2 text-sm font-black disabled:opacity-40">Move Down</button></div></div><div className="grid gap-4 md:grid-cols-2"><Field label="Title" value={service.title ?? ""} onChange={(value) => updateLocal(service.id, { title: value })} /><Field label="Price" value={String(service.price ?? "")} onChange={(value) => updateLocal(service.id, { price: Number(value) })} type="number" /><Field label="Duration" value={String(service.duration ?? "")} onChange={(value) => updateLocal(service.id, { duration: Number(value) })} type="number" /><Field label="Listing Order Number" value={String(service.sort_order ?? (index + 1) * 10)} onChange={(value) => updateLocal(service.id, { sort_order: Number(value) })} type="number" /><label className="grid gap-2 md:col-span-2"><span className="text-sm text-zinc-400">Description</span><textarea value={service.description ?? ""} onChange={(event) => updateLocal(service.id, { description: event.target.value })} className="min-h-24 rounded-xl border border-white/10 bg-black px-4 py-3" /></label></div><div className="mt-5 flex flex-wrap gap-3"><button disabled={activeId === service.id} onClick={() => saveService(service)} className="rounded-full border border-green-500 px-5 py-2 text-green-400 disabled:opacity-50">Save Service</button><button disabled={activeId === service.id} onClick={() => deleteService(service.id)} className="rounded-full border border-red-500 px-5 py-2 text-red-500 disabled:opacity-50">Delete Service</button></div><div className="mt-8 rounded-3xl border border-white/10 bg-black/40 p-5"><h3 className="text-xl font-black">Variations</h3>{serviceVariations.length === 0 ? <p className="mt-3 text-sm text-zinc-500">No variations for this service yet.</p> : <div className="mt-5 grid gap-4">{serviceVariations.map((variation) => <div key={variation.id} className="rounded-2xl border border-white/10 bg-white/5 p-4"><div className="grid gap-3 md:grid-cols-2"><Field label="Variation Name" value={variation.variation_name ?? ""} onChange={(value) => updateLocalVariation(variation.id, { variation_name: value })} /><Field label="Price" value={String(variation.price ?? "")} onChange={(value) => updateLocalVariation(variation.id, { price: Number(value) })} type="number" /><Field label="Duration" value={String(variation.duration ?? "")} onChange={(value) => updateLocalVariation(variation.id, { duration: Number(value) })} type="number" /></div><div className="mt-4 flex flex-wrap gap-3"><button disabled={activeId === variation.id} onClick={() => saveVariation(variation)} className="rounded-full border border-green-500 px-5 py-2 text-green-400 disabled:opacity-50">Save Variation</button><button disabled={activeId === variation.id} onClick={() => deleteVariation(variation.id)} className="rounded-full border border-red-500 px-5 py-2 text-red-500 disabled:opacity-50">Delete Variation</button></div></div>)}</div>}</div></div>; })}</section>}</div></main></AdminUnlockGate>;
}

function Field({ label, value, onChange, type = "text" }: { label: string; value: string; onChange: (value: string) => void; type?: string }) { return <label className="grid gap-2"><span className="text-sm text-zinc-400">{label}</span><input type={type} value={value} onChange={(event) => onChange(event.target.value)} className="rounded-xl border border-white/10 bg-black px-4 py-3" /></label>; }
